<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Сайты</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="/">Анализатор страниц</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/">Главная</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/urls">Сайты</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<main class="container">
    <h1>Сайты</h1>

    <!-- Флэш-сообщения -->
    <?php if (!empty($flash['success'])): ?>
        <?php foreach ($flash['success'] as $message): ?>
            <div class="alert alert-success"><?= htmlspecialchars($message) ?></div>
        <?php endforeach; ?>
    <?php endif; ?>
    <?php if (!empty($flash['error'])): ?>
        <?php foreach ($flash['error'] as $message): ?>
            <div class="alert alert-danger"><?= htmlspecialchars($message) ?></div>
        <?php endforeach; ?>
    <?php endif; ?>

    <!-- Форма добавления сайта -->
    <form method="post" action="/urls" class="mb-4" data-test="url-form">
        <div class="input-group">
            <input
                    type="text"
                    required
                    name="url[name]"
                    value="<?= htmlspecialchars($url ?? '') ?>"
                    class="form-control"
                    placeholder="https://example.com"
                    data-test="url-input"
            >
            <button type="submit" class="btn btn-primary" data-test="submit">Добавить</button>
        </div>
    </form>

    <!-- Таблица сайтов -->
    <table class="table" data-test="urls">
        <thead>
        <tr>
            <th>ID</th>
            <th>Имя</th>
            <th>Дата создания</th>
            <th>Код ответа</th>
            <th>Дата последней проверки</th>
        </tr>
        </thead>
        <tbody>
        <?php foreach ($urls as $url): ?>
            <tr>
                <td><?= $url['id'] ?></td>
                <td>
                    <a href="/urls/<?= $url['id'] ?>"><?= htmlspecialchars($url['name']) ?></a>
                </td>
                <td><?= $url['created_at'] ?></td>
                <td><?= $url['last_status_code'] ?? '' ?></td>
                <td><?= $url['last_check'] ?? '' ?></td>
            </tr>
        <?php endforeach ?>
        </tbody>
    </table>
</main>
</body>
</html>
